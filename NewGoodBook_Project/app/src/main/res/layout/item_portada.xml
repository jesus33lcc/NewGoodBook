<?xml version="1.0" encoding="utf-8"?>
<!-- Portada seleccionable del onboarding. La marca de elegido es un borde dorado y
     un velo: sin el velo, sobre portadas claras el borde apenas se distinguia. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="6dp">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/tarjetaPortada"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        app:cardCornerRadius="6dp"
        app:cardElevation="4dp"
        app:strokeWidth="0dp">

        <ImageView
            android:id="@+id/portadaElegible"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/desc_portada"
            android:scaleType="centerCrop" />

        <View
            android:id="@+id/veloElegido"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#66E89D10"
            android:visibility="gone" />
    </com.google.android.material.card.MaterialCardView>

</FrameLayout>
